{
  "name": "crazygrowmind-studio",
  "private": true,
  "version": "1.0.0",
  "description": "Root convenience scripts — runs backend + admin together for local development",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix admin",
    "dev": "concurrently -n BACKEND,ADMIN -c blue,yellow \"npm run dev --prefix backend\" \"npm run dev --prefix admin\"",
    "seed": "npm run seed --prefix backend",
    "seed:branding": "npm run seed:branding --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
